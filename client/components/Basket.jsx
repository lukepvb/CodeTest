import React, { useState } from 'react';
import { Button, Form, FormGroup, Label, Input, FormText } from 'reactstrap';

const Basket = (props) => {
  return (
    <div>
      <h1>Lots of baskets these days</h1>
    </div>
  );
};

export default Basket;
